# Every lint or type-check suppression standing in this repository.
#
# This file is the ONLY way to suppress a diagnostic here. `just check-repo`
# refuses a suppression directive in the tree with no entry below, an entry
# whose reason is absent or empty, an entry that matches no directive, and — on
# a change, against its base revision — a directive introduced without its entry
# in the same change.
#
# Nothing here asks whether a reason is a GOOD reason. That is a question for
# whoever reviews the change; a check that tried to answer it would be exactly
# the non-deterministic gate this file exists to replace.
#
# An entry matches a directive when `file` is the file the directive is in,
# `rule` is the rule the directive names, and `site` occurs on the directive's
# own line or in one of the three following non-blank lines.

schema_version = 1

# --- WHOLE-FILE llmlint directives --------------------------------------------
#
# The two entries per script below are `llmlint: ignore-file` directives, which
# cover the whole script rather than one line. That is permitted only because
# both rules are about a file as a whole rather than a line, and only for the
# rules `repo-policy.toml`'s suppressions.whole_file_rules names; `just
# check-repo` refuses the form from any other tool and for any other rule. Each
# still needs the entry and reason below like any other suppression.
#
# --- scripts/session-setup.sh -------------------------------------------------
# The reason is the script's own header: a SessionStart hook that aborted a
# session because an install was flaky would be worse than one that logs and
# continues.

[[suppression]]
rule = "tool_output_is_signal"
file = "scripts/session-setup.sh"
site = "set -uo pipefail"
reason = "Whole-file, and the rule is about the whole file: success is quiet and every failure logs and continues rather than blocking startup, so this script's output is deliberately advisory rather than a gate's verdict."

[[suppression]]
rule = "boundary_inputs_validated"
file = "scripts/session-setup.sh"
site = "set -uo pipefail"
reason = "Whole-file, and the rule is about the whole file: the only external input this script takes is the `rust-just` wheel from PyPI, whose releases carry Trusted Publishing and PEP 740 attestations, so no untrusted input is executed anywhere in it."

# --- scripts/setup-llmlint.sh -------------------------------------------------

[[suppression]]
rule = "tool_output_is_signal"
file = "scripts/setup-llmlint.sh"
site = "set -uo pipefail"
reason = "Whole-file, and the rule is about the whole file: success is quiet and a failed install logs an actionable line and continues rather than blocking startup."

[[suppression]]
rule = "boundary_inputs_validated"
file = "scripts/setup-llmlint.sh"
site = "set -uo pipefail"
reason = "Whole-file, and the rule is about the whole file: every external input it takes is a named package from a registry the ecosystem authenticates — the `llmlint-cli` wheel from PyPI (Trusted Publishing and PEP 740 attestations) and the `@anthropic-ai/claude-code` package from npm (provenance attestations) — and each package name is a constant in the file rather than anything a caller supplies, so no untrusted input is executed anywhere in it."

# --- the single subprocess site ---------------------------------------------

[[suppression]]
rule = "S603"
file = "tools/repo-checks/src/repo_checks/shell.py"
site = "subprocess.run"
reason = "S603 fires on `subprocess.run` itself whatever it runs, and no code change silences it. Every check, command and test helper is funnelled through this one call so the rule has one reviewable site instead of thirty-four; the executable is resolved with `shutil.which` first (so S607 is fixed rather than suppressed), the arguments are always a list, and nothing here runs through a shell."

[[suppression]]
rule = "S603"
file = "tools/repo-checks/src/repo_checks/shell.py"
site = "subprocess.Popen"
reason = "The same rule at the same module's second and last call, for the same reason: some of what this repository drives is a server a journey then makes requests of, and `run` waits. Every long-running process a check, command or journey starts goes through this one call — the executable is resolved with `shutil.which` first (so S607 is fixed rather than suppressed), the arguments are always a list, and nothing here runs through a shell."

# --- the one call this repository's own journey has to make wrongly ---------

[[suppression]]
rule = "missing-argument"
file = "python/printobserver-sdk/integration/test_journey.py"
site = "client.pause(world.print_id)"
reason = "The nine-step journey's seventh step is that a mutating call with no reason is refused, and in this client the reason can be left out at run time where the two typed clients refuse it at compile time. Proving that refusal means making the call the type checker is right to object to, once, at the one site that asserts it is refused."

# --- the scripted OctoPrint environment's one subprocess site ---------------

[[suppression]]
rule = "S603"
file = "tools/octoprint-env/octoprint_env.py"
site = "subprocess.Popen"
reason = "S603 fires on the subprocess call itself whatever it runs, and no code change silences it. Every process this script starts — `uv`, and OctoPrint's own program — goes through this one helper, so the rule has one reviewable site rather than one per caller; each `argv[0]` is an absolute path this script composed (so S607 is fixed rather than suppressed), the arguments are always a list, and nothing runs through a shell."

# --- the self-hosted Obico environment's one subprocess site ----------------

[[suppression]]
rule = "S603"
file = "tools/obico-env/obico_env.py"
site = "subprocess.Popen"
reason = "S603 fires on the subprocess call itself whatever it runs, and no code change silences it. Every process this script starts — `docker` and `git`, and nothing else — goes through this one helper, so the rule has one reviewable site rather than one per caller; each `argv[0]` is resolved against PATH by `_resolve` first (so S607 is fixed rather than suppressed), the arguments are always a list, and nothing runs through a shell."

# --- the scheduled Obico tier's one URL-opening site ------------------------

[[suppression]]
rule = "S310"
file = "tools/obico-env/obico_tier.py"
site = "urllib.request.Request(url"
reason = "S310 fires on building a request for a URL whose scheme it cannot audit statically, and no code change silences it for a URL that is data. Every URL this tier opens goes through this one helper, so the rule has one reviewable pair of lines rather than one per caller: the addresses are the webhook and control addresses the caller named on the command line, and the `img_url` the captured body carries — which is the very thing this tier exists to report on, so refusing to look at it would defeat the tier."

[[suppression]]
rule = "S310"
file = "tools/obico-env/obico_tier.py"
site = "urllib.request.urlopen(request"
reason = "The second half of the same one URL-opening site: S310 flags both building the request and opening it, and this is the single `urlopen` every fetch in this tier goes through, immediately below the `Request` it was handed."

# --- the scheduled Obico tier's suite: two URL-opening sites ----------------

[[suppression]]
rule = "S310"
file = "tools/obico-env/tests/harness.py"
site = "urllib.request.Request(url)"
reason = "S310 fires on building a request for a URL whose scheme it cannot audit statically. This is the suite's own HTTP client, and every address reaching it is one this suite composed from the record the bring-up wrote — `http://localhost:<port>` on loopback — so there is no caller-supplied scheme to audit. One helper rather than one call per journey, so the rule has one reviewable site."

[[suppression]]
rule = "S310"
file = "tools/obico-env/tests/harness.py"
site = "urllib.request.urlopen(request"
reason = "The second half of the same one site: S310 flags both building the request and opening it, and this is the single `urlopen` every read in this suite goes through."

[[suppression]]
rule = "S310"
file = "tools/obico-env/tests/substitute.py"
site = "self.webhook_url,"
reason = "S310 fires on building a request for a URL whose scheme it cannot audit statically. This is the stand-in posting to the webhook address the journey that built it named, which is always a loopback `http://127.0.0.1:<free port>/alert` composed in the journey itself."

[[suppression]]
rule = "S310"
file = "tools/obico-env/tests/substitute.py"
site = "urllib.request.urlopen(request"
reason = "The second half of the same one site: S310 flags both building the request and opening it, and this stand-in posts through exactly one `urlopen`."

[[suppression]]
rule = "async_typed_clients_at_boundaries"
file = "tools/octoprint-env/octoprint_env.py"
site = "http.client.HTTPConnection"
reason = "Every call through this helper reaches an OctoPrint this script itself provisioned and started, on loopback at a port it claimed itself, and each one is a step of a strictly ordered bring-up — is one already answering, wait until the API answers, wait until the printer connects, upload the hold print, watch the job start — whose answer is acted on before the next request is made. There is no concurrency for an async client to win, no official async typed OctoPrint client to adopt, and satisfying the rule would mean writing an async HTTP client and putting an event loop under a provisioning script whose whole contract is one blocking command. The boundary this rule is about is the product's, and the product reaches OctoPrint from the `printobserver-octoprint` crate rather than from this script."

[[suppression]]
rule = "async_typed_clients_at_boundaries"
file = "tools/octoprint-env/tests/environment.py"
site = "http.client.HTTPConnection"
reason = "This is the tier's own observation helper, deliberately sharing no code with the script it observes, and it polls one loopback instance the tier brought up — either the journey's own `up`, or the one `just octoprint-up` left running — one observation at a time, with each answer asserted on before the next call is made. Making it async would put an event loop and an async test framework under a suite whose assertions are strictly sequential, without a single call gaining anything. The boundary this rule is about is the product's, which is the `printobserver-octoprint` crate."

[[suppression]]
rule = "clippy::too_many_lines"
file = "crates/printobserver/tests/journeys/documented.rs"
site = "pub fn operator_workflow("
reason = "The function is the six steps of one supervision turn, written in the order the skill states them, and each step's inputs are what the steps before it resolved out of the documentation. Splitting it would put the reader's own sequence in six places and leave nothing carrying the turn, which is the one thing this journey is about."


[[suppression]]
rule = "changed_behavior_has_e2e"
file = "crates/printobserver-server/src/server.rs"
site = "working_dir: assets.clone(),"
reason = "A wrong working directory makes the agent's relative reference-file opens fail with a missing-path error on its first documentation read. That failure is visible to the supervising agent, not silent, so the rule's silent-failure condition is not met. The installed-assets journey already proves the bundled links resolve without a checkout; a billed harness turn would re-prove file resolution."

[[suppression]]
rule = "changed_behavior_has_e2e"
file = ".config/nextest.toml"
site = "[test-groups]"
reason = "Assigning binary(integration) to octoprint with max-threads = 1 is a structural declaration settled by reading this file. The narrowed rule excludes structural claims; another runtime test would not improve that proof."

[[suppression]]
rule = "boundary_inputs_validated"
file = "crates/printobserver/tests/journeys/documented.rs"
site = "let body = std::fs::read_to_string(beside.join(&target))"
reason = "Checkout links intentionally traverse the assets/reference symlink into docs/reference outside the skill directory. The docs skill check validates the declared bundle and the installed-assets journey verifies materialized links. Directory confinement would reject the supported checkout layout."

[[suppression]]
rule = "changed_behavior_has_e2e"
file = "crates/printobserver/tests/journeys/documenting.rs"
site = "if writing() {"
reason = "Proving replacement of stale committed examples requires a corrupted documentation copy, forbidden by this dispatch. The positive journey executes and compares every documented example against a real server; write mode uses those captured steps."

[[suppression]]
rule = "changed_behavior_has_e2e"
file = "justfile"
site = "docs-generate:"
reason = "Proving repair of an incomplete generated documentation set requires forbidden tree surgery. The real schema-writing CLI test, runtime surface comparison and real-server documented-example journey remain."

[[suppression]]
rule = "expensive_tests_stay_behind_their_own_edge"
file = "crates/printobserver/project.json"
site = "test-integration"
reason = "Repository policy requires the OctoPrint tier on every change on both platforms outside the fast gate. A different affected edge cannot narrow that schedule, and changing the schedule or adding CI jobs is outside this dispatch."

[[suppression]]
rule = "changed_behavior_has_e2e"
file = "crates/printobserver/project.json"
site = "metadata"
reason = "Proving regeneration from a missing or corrupted committed surface requires forbidden tree surgery. The existing reference test compares the committed manifest to the actual runtime surface serialized by write mode."

[[suppression]]
rule = "invalid_states_unrepresentable"
file = "crates/printobserver-oneharness/src/lib.rs"
site = "pub const REFERENCE_DIRECTORY:"
reason = "The immutable literal reference is not a field or caller-supplied path, so callers cannot assign escaping values. The documentation bundle check holds consumers to the declared layout."

[[suppression]]
rule = "invalid_states_unrepresentable"
file = "crates/printobserver-oneharness/src/lib.rs"
site = "pub const DEFAULT_REFERENCES:"
reason = "The immutable array carries literal bundled paths rather than caller-constructed entries. The documentation bundle check compares every path with the validated policy set in both directions, and the installed-assets journey opens the materialized references."

[[suppression]]
rule = "instruction_layer_localized"
file = "AGENTS.md"
site = "Keep the OctoPrint journey in the required gate"
reason = "This is a repository-wide gate and merge-protection constraint spanning the e2e project and CI workflows; neither project subtree alone owns where the required gate runs."

[[suppression]]
rule = "agents_md_durable_and_terse"
file = "AGENTS.md"
site = "The rule above has one recorded exception, and it is closed"
reason = "Required content rather than session history. The release-path repair that wrote this passage was required to record, where a reader meets the rule it departs from, three things: why the workspace version was moved by hand, that it is a single closed exception, and that control returns to release automation — and the passage is those three statements and the journey that holds the tree to them, nothing more. Each is confirmable from the tree: commit 3952add moved the workspace from 0.1.0 to 0.2.0 by hand on 2026-09-10, `manifests.automation_owned` in repo-policy.toml is the rule it departs from, and `tests/repo-e2e/tests/test_release_path_journey.py` refuses a tree at a tagged version. The durable instruction the rule would keep — do not repeat this, fix the state instead — is unreadable without the exception it refuses to repeat, so the date and the cause are what make the prohibition followable rather than narrative around it."

[[suppression]]
rule = "instruction_layer_localized"
file = "CLAUDE.md"
site = "Keep the OctoPrint journey in the required gate"
reason = "CLAUDE.md is the AGENTS.md symlink, so the same repository-wide gate and merge-protection constraint is scanned through this path too; neither the e2e nor CI subtree alone owns it."

[[suppression]]
rule = "agents_md_durable_and_terse"
file = "CLAUDE.md"
site = "The rule above has one recorded exception, and it is closed"
reason = "CLAUDE.md is the AGENTS.md symlink, so the same passage is scanned through this path too; the AGENTS.md entry above for this rule and site carries the reason, and it holds unchanged here."

[[suppression]]
rule = "tool_output_is_signal"
file = "justfile"
site = "test-install-proof:"
reason = "A passing `just test-install-proof` prints three lines, and they are three independent proofs rather than one proof said three times: each of the three route recipes it runs proves a different route against a different registry, and its one line carries which version that registry served, where that version was selected from, what installed it and what the installed program answered — the evidence this tier exists to produce, and one a summary line cannot carry for three routes at once. `tests/repo-e2e/tests/test_registry_proof_journey.py::test_the_tier_recipe_proves_every_route` asserts exactly those three lines and nothing beside them. A route that fails still prints its whole report on the other stream and stops the tier there, so a failed run is not three lines either."

[[suppression]]
rule = "instruction_layer_localized"
file = "tools/release-artifacts/AGENTS.md"
site = "# release-artifacts"
reason = "The rule asks that project rules live in a nested `AGENTS.md` and that `CODEOWNERS` routes reviews, and both hold: this file is that nested one, and `.github/CODEOWNERS` assigns every path with `* @nickderobertis`. The judged tier runs over a diff and is handed only the files that changed, so that unchanged file is outside what it reads — run `20260910T235643Z-7ce94` listed `.github/CODEOWNERS` among its `diff_excluded_files` and then failed this file for having no routing. The same diff passed this rule on the two runs before it; this directive makes the answer not depend on which files a diff happens to carry."

[[suppression]]
rule = "S310"
file = "tests/repo-e2e/tests/test_release_tag_journey.py"
site = "urllib.request.Request(url"
reason = "S310 fires on building a request for a URL whose scheme it cannot audit statically, and no code change silences it. Every URL this journey opens goes through this one helper, so the rule has one reviewable pair of lines rather than one per caller: the stand-in registry forwarding an index read to `https://index.crates.io` — a literal this module owns — and the journey driving the stand-in forge on the loopback `http://127.0.0.1:<port>` it composed itself."

[[suppression]]
rule = "S310"
file = "tests/repo-e2e/tests/test_release_tag_journey.py"
site = "urllib.request.urlopen(request"
reason = "The second half of the same one URL-opening site: S310 flags both building the request and opening it, and this is the single `urlopen` every exchange in this journey goes through, immediately below the `Request` it was handed."

[[suppression]]
rule = "S310"
file = "tools/release-artifacts/src/release_artifacts/registries.py"
site = "urllib.request.Request("
reason = "S310 fires on building a request for a URL whose scheme it cannot audit statically, and no code change silences it. Every read this proof makes of a registry goes through this one helper, so the rule has one reviewable pair of lines rather than one per registry — and the scheme IS audited, on the line above the directive: a URL that is not `http` or `https` is refused before anything is asked over it. The addresses are the three registry bases this repository declares, or the one stand-in address a caller pointed them at."

[[suppression]]
rule = "S310"
file = "tools/release-artifacts/src/release_artifacts/registries.py"
site = "urllib.request.urlopen(request"
reason = "The second half of the same one URL-opening site: S310 flags both building the request and opening it, and this is the single `urlopen` every read of a registry goes through, immediately below the `Request` it was handed."

[[suppression]]
rule = "S324"
file = "tools/release-artifacts/src/release_artifacts/standin.py"
site = "hashlib.sha1(raw)"
reason = "S324 is right that SHA-1 is not a hash to choose, and this one is not chosen: `dist.shasum` in a packument of the JavaScript registry is SHA-1 by that registry's own protocol, and a stand-in serving anything else is one `npm` refuses to install from. It is a digest an installer checks a tarball against rather than a security decision of this repository, and the `integrity` field beside it — which is what a modern `npm` prefers — carries SHA-512."

[[suppression]]
rule = "test_tiers_split_by_project_not_by_marker"
file = "tools/release-artifacts/tests/test_registries.py"
site = "llmlint: ignore[test_tiers_split_by_project_not_by_marker]"
reason = "The judged tier read this suite as expensive work that belongs in a tier of its own. It is where its neighbours are: `test_taking.py` in the same directory installs all three routes for real and `test_clients.py` builds the `printobserver` program and starts a real supervisor, so what edges this project's `test` target is what it proves rather than what it costs. Moving one of the four out would split identical work across two projects, and it would leave `registries.py` and `standin.py` measured by no tier that carries a coverage floor — the end-to-end tier is not measured. The end-to-end journeys over the same behaviour, which drive the real recipes, are in `tests/repo-e2e` where that tier's own work belongs."

[[suppression]]
rule = "tests_mirror_real_usage"
file = "tests/repo-e2e/tests/test_release_path_journey.py"
site = "llmlint: ignore[tests_mirror_real_usage]"
reason = "The committed step is `release-plz release-pr`, and that command is `release-plz update` followed by opening a pull request on a forge: the computation this journey exists to prove — each package's next version, compared against what the registry serves — is the whole of `update`, and the pull request is the one part a journey cannot drive, because it needs a forge and a token and leaves a pull request on a real repository. So the journey runs the real program's real computation against a real (stand-in) registry over a real copy, and stops before the one step that is somebody else's server. Driving `release-pr` instead would prove the same computation and then fail on the forge whatever the tree looked like."

[[suppression]]
rule = "contracts_have_one_source_or_a_drift_gate"
file = "tools/release-artifacts/src/release_artifacts/registries.py"
site = "llmlint: ignore[contracts_have_one_source_or_a_drift_gate]"
reason = "The sample is the one source in this repository of what `release-plz release --output json` answers, and every fixture and the reader are built from it; what it cannot have is a drift gate against a live release-plz, because that program writes this answer only having released something — which needs a forge, a registry and a publish, none of which a tier here may do. The reconciliation is the real release path itself: a shape that moved fails `just release-answer` in the `release` job naming the field, rather than reading as `released nothing`, and the same journey (`tests/repo-e2e/tests/test_release_program.py`) proves the committed argument list, `--output json` included, is one the installed program accepts. Recorded with the release-plz revision the shape was read from, so the day it moves a reader knows what to re-read."

[[suppression]]
rule = "e2e_not_mocked"
file = "tests/repo-e2e/tests/test_release_path_journey.py"
site = "llmlint: ignore[e2e_not_mocked]"
reason = "What stands in is crates.io, and for the one reason the install proof's `standin.py` stands the package registries in: nothing in this repository may publish to a registry to prove a point, and the state this journey proves — a registry carrying no entry for a crate this tree publishes — is one the real registry stops being in the moment the release goes out. Everything else is real: the real `release-plz` program, its real computation, the real copy of the tree, the real tag, and the sparse-index protocol cargo itself speaks to the stand-in over HTTP. The layer under test is the drafting tool over this tree's versions, and it is not what is substituted."

[[suppression]]
rule = "expensive_tests_stay_behind_their_own_edge"
file = "tests/repo-e2e/tests/test_release_path_journey.py"
site = "llmlint: ignore[expensive_tests_stay_behind_their_own_edge]"
reason = "The same ground the entries for this rule at `tests/repo-e2e/project.json` and `tools/release-artifacts/tests/test_registries.py` state: this repository's end-to-end tier is one project by design, `AGENTS.md` places this coverage there, and no per-project end-to-end target exists anywhere for a narrower edge to hang from. A project of its own would carry the same `test-e2e` target name and be run by the same `nx run-many` the tier already is, so there is no placement that would spare an unrelated change this journey. A reader can confirm both from the tree: `tests/repo-e2e/project.json` is the only project declaring a `test-e2e` target, and the `test-e2e` recipe in the justfile runs the tier through `nx run-many`."

[[suppression]]
rule = "e2e_not_mocked"
file = "tests/repo-e2e/tests/test_release_tag_journey.py"
site = "class StandInRegistry"
reason = "What stands in is crates.io's WRITE path, for the one reason the release-path journey's `EmptyIndex` stands its read path in: nothing in this repository may publish to a registry to prove a point, and the state this journey proves — two of thirteen crates on the registry and no tag on the forge — is one the real registry leaves the moment the release completes. Everything the stand-in takes is real: the real `cargo publish` upload the real run performed, the sparse-index protocol and the download path cargo itself speaks to it over HTTP, and every third-party crate read forwarded to the real crates.io, because cargo resolves this workspace's siblings from crates.io whatever `--registry` names and a stand-in that owned only the siblings would resolve nothing. The layer under test is the release program's tagging over this configuration, and it is not what is substituted."

[[suppression]]
rule = "e2e_not_mocked"
file = "tests/repo-e2e/tests/test_release_tag_journey.py"
site = "class StandInForge"
reason = "What stands in is GitHub, and for the same reason: a journey may not create a tag or a release on the real repository to prove that the second one is refused. The stand-in answers the four endpoints `release-plz release` touches with GitHub's own status codes, refuses a second `POST …/git/refs` for one ref with `422 Reference already exists` exactly as GitHub does — which is the answer the `0.2.0` release died on — and records every write in the same ordered record the registry writes to, so the ordering the change promises (the tag after the last upload) is read off one sequence rather than argued from timestamps. Its refusal is held by a journey of its own, driven over HTTP."

[[suppression]]
rule = "contracts_have_one_source_or_a_drift_gate"
file = "tests/repo-e2e/tests/test_release_tag_journey.py"
site = "REFERENCE_EXISTS = ("
reason = "The constant IS the one source, and it is a recording rather than a restatement: the response body GitHub answered the real release of 0.2.0 with, read verbatim from the `release` job's log of workflow run 34578287017 on 2026-09-11 (`gh run view 34578287017 --log`, the `Response body:` line release-plz prints when a forge refuses it), with that provenance written beside it. What it reconciles is reconciled: the forge stand-in serves the recording and the former-configuration journey holds the real release-plz to reporting it exactly as that run's log did. What the rule still asks for — a drift gate against the live endpoint — has one possible implementation, creating one ref twice on github.com/nickderobertis/printobserver, and no tier here may write to the real forge; the day GitHub changes this body, the real release path is where it shows, exactly as `tools/release-artifacts/src/release_artifacts/registries.py`'s entry for this rule records for release-plz's answer shape. The directive sits on the constant itself so the site is named rather than covered by the stand-in's class-level entry."

[[suppression]]
rule = "contracts_have_one_source_or_a_drift_gate"
file = "tests/repo-e2e/tests/test_release_tag_journey.py"
site = "class StandInRegistry"
reason = "What this stand-in restates is cargo's registry web API and sparse-index format, and the drift gate is cargo itself: the real `cargo publish` uploads to it and the real `cargo info` reads its index and downloads from it on every run of this journey, so a frame, a route or an index field cargo stopped speaking fails the journey at the boundary rather than being approximated. There is no machine-readable source of that protocol to generate from — cargo documents it in prose — and the one consumer that matters is the one driven here."

[[suppression]]
rule = "contracts_have_one_source_or_a_drift_gate"
file = "tests/repo-e2e/tests/test_release_tag_journey.py"
site = "class StandInForge"
reason = "What this stand-in restates is the four GitHub endpoints `release-plz release` touches and GitHub's answers to them, and the consumer under test is the real release-plz, so a route or a status it stopped accepting fails the journey naming it. What it cannot have is a drift gate against GitHub, for the same reason `tools/release-artifacts` cannot: the behaviour it models — a second ref refused with `422 Reference already exists` — can only be reconciled by creating a tag on the real repository twice, and no tier here may write to the real forge. The reconciliation is the real release path: the same program creates the same ref on GitHub on every push to `main` that cuts a release."

[[suppression]]
rule = "async_typed_clients_at_boundaries"
file = "tests/repo-e2e/tests/test_release_tag_journey.py"
site = "urllib.request.Request(url"
reason = "The one place this journey opens a URL, and neither half of the rule applies to it. It is called from inside a synchronous `http.server` handler — the stand-in registry forwarding an index read to crates.io while cargo waits on that very request — and from a journey whose assertions are strictly sequential over a loopback forge it stood up itself; an awaited client under either would need an event loop with nothing to overlap. The typed half is answered where the answer is consumed: the forwarded body is handed back to cargo byte for byte, which is the one consumer that validates a sparse-index entry, and the forge's answer is narrowed to a status and a body the journey asserts on."

[[suppression]]
rule = "async_typed_clients_at_boundaries"
file = "tests/repo-e2e/tests/test_release_tag_journey.py"
site = "urllib.request.urlopen(request"
reason = "The second half of the same one site as the entry above: the rule reads the request's construction and its opening as two, and this is the single `urlopen` every exchange in this journey goes through, immediately below the `Request` it was handed."

[[suppression]]
rule = "tests_mirror_real_usage"
file = "tests/repo-e2e/tests/test_release_tag_journey.py"
site = "def released"
reason = "The argument list is the committed step's own, read through `release_step_arguments()` rather than restated, and what is added is routing and one verification skip: `--registry`, `--token`, `--repo-url` and `--git-token` point the program at the two stand-ins on loopback, and `--no-verify` skips the release build every `cargo publish` otherwise performs of the crate it just packaged — which is cargo's verification of the packaged source, proven elsewhere by the build tier, and not what this journey is about. Without it thirteen release-mode builds of the workspace would sit between the journey and the one thing it asks: which package creates the tag, and when."

[[suppression]]
rule = "expensive_tests_stay_behind_their_own_edge"
file = "tests/repo-e2e/tests/test_release_tag_journey.py"
site = "def released"
reason = "The same ground the entries for this rule at `tests/repo-e2e/project.json` and `tests/repo-e2e/tests/test_release_path_journey.py` state: this repository's end-to-end tier is one project by design, `AGENTS.md` places this coverage there, and no per-project end-to-end target exists for a narrower edge to hang from. What the journey costs — eleven real `cargo publish` uploads to a loopback registry, about a minute — is already bounded by `--no-verify` and by seeding with a real publish rather than a build; the remaining cost is the release program doing the thing under test."

[[suppression]]
rule = "e2e_not_mocked"
file = "tests/repo-e2e/tests/test_release_workflow_journey.py"
site = "llmlint: ignore[e2e_not_mocked]"
reason = "The layer under test is the committed workflow's own scheduling — needs, gates, outputs — and that is what runs unsubstituted, over the workflow's own text with the real `just release-answer` recipe over a real copy. What is stood in reaches outside this host: `release-plz` publishes to crates.io and a forge, `just publish-artifacts` publishes to PyPI and npm, and `just build-artifacts` and `just bootstrap` are a cold release build of the workspace per matrix cell and a toolchain install, which the question — are these two recipes REACHED — does not need run to be answered. The stand-in `release-plz` behaves as the real one does at the point that matters: it prints its answer only when asked with `--output json`, so the journey that drops that option fails the job the way the real program would."

[[suppression]]
rule = "tests_mirror_real_usage"
file = "tests/repo-e2e/tests/test_release_workflow_journey.py"
site = "llmlint: ignore[tests_mirror_real_usage]"
reason = "The interface that schedules a workflow is GitHub Actions, and it cannot be fired from a journey — the same reason the registry-proof and service-report journeys read the committed workflow. This one goes further than reading: it runs the committed workflow's own text under the forge's documented scheduling rules (`actions.py`), so what is asserted is what those jobs did — ran, were skipped, failed, and what each published — rather than what the YAML says. Nothing in the assertions names the runner's internals; the run object reports results by the workflow's own job keys."

[[suppression]]
rule = "tests_mirror_real_usage"
file = "tests/repo-e2e/tests/test_actions_runner.py"
site = "llmlint: ignore[tests_mirror_real_usage]"
reason = "`actions.py` is a test helper whose whole interface is `Runner.run()` and `evaluate()`, and this module is where its claims about the forge are pinned over a workflow small enough to read. There is no other interface to reach it through: the forge itself is the thing it stands in for and cannot be fired from a test. The release-workflow journey is what uses it over the committed workflow."

[[suppression]]
rule = "contracts_have_one_source_or_a_drift_gate"
file = "tests/repo-e2e/tests/actions.py"
site = "llmlint: ignore[contracts_have_one_source_or_a_drift_gate]"
reason = "The forge's scheduling rules are documented prose with no machine-readable source to generate from, and the drift gate is the forge itself: the same workflow text runs on GitHub on every push to the base branch, which is where a rule modelled here wrongly shows. What keeps the model honest in the meantime is that it refuses by name every construct and expression outside the subset it models — a workflow that grows past the subset fails the journey rather than being approximated — and `test_actions_runner.py` pins each modelled rule over a workflow small enough to read against the forge's documentation."

[[suppression]]
rule = "async_typed_clients_at_boundaries"
file = "tools/release-artifacts/src/release_artifacts/registries.py"
site = "urllib.request.Request("
reason = "The typed half is answered rather than suppressed: every answer a registry gives is narrowed at this boundary and a body that is not the document its own protocol serves is refused there. The async half does not apply to this program. It is a command-line tool a `just` recipe runs to completion, with no event loop anywhere in it: every subprocess, every install and every read it makes goes through `repo_checks.shell.run` synchronously and in order, because a proof of one route is a sequence — ask, install, run, read back. One awaited read inside it would need a loop this program has no reason to have, and would not make the proof any more concurrent, because there is nothing to overlap it with."

[[suppression]]
rule = "live_tier_compiles_and_requires_credential"
file = ".github/workflows/install-path.yml"
site = "prove-registry-pypi:"
reason = "There is no credential to require, and that is the point of the tier rather than an omission in it: what it proves is the ANONYMOUS path an end user takes, and a registry read authenticated by a token would prove a path nobody in front of a printer has. The failure this rule guards against cannot happen here either — the tier does not skip when something is missing. An unreachable or refusing registry is a non-zero exit naming that registry, a registry serving nothing for the version under test is `NOT SERVED`, and only `SERVED AND PROVEN` exits zero."

[[suppression]]
rule = "live_tier_compiles_and_requires_credential"
file = ".github/workflows/install-path.yml"
site = "prove-registry-npm:"
reason = "The same tier and the same reason as the job above it: the JavaScript registry is read anonymously because that is how the end user's own `npm install -g` reads it."

[[suppression]]
rule = "live_tier_compiles_and_requires_credential"
file = ".github/workflows/install-path.yml"
site = "prove-registry-script:"
reason = "The same tier and the same reason as the two jobs above it: the forge's releases and their artifacts are read anonymously because that is how the committed install script reads them on the machine beside the printer."

[[suppression]]
rule = "expensive_tests_stay_behind_their_own_edge"
file = "tools/release-artifacts/tests/test_registries.py"
site = "llmlint: ignore[expensive_tests_stay_behind_their_own_edge]"
reason = "The same placement as the directive above it, and the same reason: this suite is where its neighbours are. There is also no edge to put it behind that the graph would honour — the gate's `test` tier is `nx run-many -t test`, so a project of its own would carry a `test` target and be run by the same tier, and the one tier that is edged differently (`test-e2e`) is not measured for coverage."

[[suppression]]
rule = "expensive_tests_stay_behind_their_own_edge"
file = "tests/repo-e2e/project.json"
site = "test-e2e"
reason = "On the target rather than in a journey, as `crates/printobserver/project.json` carries the same rule for its integration tier: what the rule is about is the edge this project's `test-e2e` target hangs from, which is decided here once and not per test — a directive over one of the registry journey's fourteen tests was read (run `20260911T000559Z-ee38c`) as leaving thirteen unanswered, and one at that module's head (run `20260911T002015Z-5c3c6`) as covering its docstring. The rule's premise does not hold in this repository: its end-to-end tier is one project by design, `AGENTS.md` places this coverage there, every journey in it is expensive by construction — bootstrapping a fresh clone, running gates, bringing OctoPrint up and waiting a print out — and there are sixteen journey modules behind one `test-e2e` target with no per-project end-to-end target anywhere for such an edge to hang from. So there is no edge to put this behind that would spare an unrelated change anything: a project of its own would carry the same target name and be run by the same `nx run-many` the tier already is. If that ceases to be true — if this repository grows per-project end-to-end targets — this suppression stops being right, and a reader can see that from this sentence."

[[suppression]]
rule = "tests_mirror_real_usage"
file = "tests/repo-e2e/tests/test_registry_proof_journey.py"
site = "llmlint: ignore[tests_mirror_real_usage]"
reason = "This one test reads the committed workflow on purpose, and it is the only way the thing it proves can be proven at all: that a release's own run is keyed on a point no release can reach before its artifacts are published, and that it takes the version from that release. A GitHub trigger cannot be fired from a journey, and waiting for a real release would mean this property was established by nobody until the next one. What it reads it then DRIVES: the selector it finds in that workflow's own `env` is what the journeys beside it pass to the real recipes against a stand-in forge. Every other journey in this file goes through `just`."

[[suppression]]
rule = "tests_mirror_real_usage"
file = "tests/repo-e2e/tests/test_service_report_journey.py"
site = "llmlint: ignore[tests_mirror_real_usage]"
reason = "The interface that produces a step's outcome is GitHub Actions, and a workflow cannot be fired from a journey — so the choice here is between driving the committed step with the two values GitHub documents it hands, and establishing nothing at all about what a completed run shows. What is driven is the REAL thing: the step's own script, read out of the committed workflow rather than restated, run by a real shell against a real summary file, and asserted on by the bytes a reader of that run would see. A copy of the line composed here would prove a line nothing publishes, which is the failure this rule is about; reading the committed one is what avoids it."

[[suppression]]
rule = "async_typed_clients_at_boundaries"
file = "tools/release-artifacts/src/release_artifacts/registries.py"
site = "urllib.request.urlopen(request"
reason = "The second half of the same one site as the entry above: the rule reads the request's construction and its opening as two, and this is the single `urlopen` every read of a registry goes through, immediately below the `Request` it was handed."

[[suppression]]
rule = "async_typed_clients_at_boundaries"
file = "tools/release-artifacts/src/release_artifacts/publishing.py"
site = '"uv", "publish"'
reason = "The client at this boundary is `uv`, which is the Python registry's own upload client and the one this repository already depends on: a wheel reaches PyPI through the legacy multipart form, signed and checked the way that client composes it, and an HTTP client written here would be a second, hand-rolled implementation of an upload protocol nobody here owns. There is no typed async Python client that publishes a wheel, and there is no event loop for an awaited one to live in — this is a command-line tool a `just` recipe and a GitHub Actions step run to completion, whose every subprocess goes through `repo_checks.shell.run` in order. The typed half is answered where it can be: what the registry SERVES is read back through `pypi_files`, which narrows that document at its own boundary and refuses anything that is not the document PyPI's protocol serves, and that read is what decides whether this publish happens at all."

[[suppression]]
rule = "async_typed_clients_at_boundaries"
file = "tools/release-artifacts/src/release_artifacts/publishing.py"
site = '"npm",'
reason = "The same ground as the entry above it, for the other registry: the client here is `npm` itself, which is how a package is published to that registry — it composes the publish document, the attachment and the integrity digests its own protocol requires, and it is the client every consumer of this artifact installs with. Replacing it with an HTTP call written here would restate that protocol in a place nothing reconciles it. The async half does not apply for the same reason: no event loop exists in this program, and one awaited publish would have nothing to overlap with. What the registry serves afterwards is read back through `npm_versions`, which narrows the packument at that boundary, and that read is what decides whether this publish happens at all."

[[suppression]]
rule = "async_typed_clients_at_boundaries"
file = "tools/release-artifacts/tests/test_publishing.py"
site = "socket.create_connection"
reason = "What this one test puts on the wire is a request no HTTP client will send: a `Content-Length` that is not a number. Every client — typed, async, or neither — composes that header itself from the body it was handed, so reaching the stand-in through one would test the client's arithmetic rather than what this stand-in does with a length it cannot read, which is the behaviour under test. A raw socket is the only way to state the malformed request, and it is one connection, opened and closed inside a strictly sequential assertion with nothing to overlap it with. Every other exchange in this suite goes through `registries.exchange`, which is the narrowed reader the rule is about."

[[suppression]]
rule = "contracts_have_one_source_or_a_drift_gate"
file = "tools/release-artifacts/src/release_artifacts/publishing.py"
site = "def publish_asset"
reason = "The same ground as the entry for `StandInForge` in `tests/repo-e2e`: what is restated here is the forge's own asset API — a `DELETE` of an asset by id, a `POST` of its bytes to the release's own upload address — and the only thing that could reconcile it is uploading an asset to a real GitHub release, which no tier of this repository may do. GitHub publishes no machine-readable document these two routes could be generated from. What stands in the place of a gate is that the release path itself runs this code on every publication: the assets a release carries are put there by this function and by nothing else, and the install-path proof reads them back from the forge afterwards, so a route or a media type GitHub stopped accepting shows as a release with no assets and a proof that says so. `AGENTS.md` states that plainly rather than leaving a reader to infer the gate is elsewhere."
