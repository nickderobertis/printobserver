"""Typed Python client for the printobserver server's HTTP surface.

This package is deliberately empty of client code: the baseline node stands up
the toolchain and the gate targets, and the `sdks` node adds the client itself
plus the distribution that publishes it.
"""

__all__: list[str] = []
