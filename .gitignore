# Local sync bookkeeping for gh-secrets (per-machine; the manifest itself is
# tracked)
.gh-secrets-state.json

# Rust
/target/
*.profraw
/lcov.info

# Python
.venv/
__pycache__/
.pytest_cache/
.ruff_cache/
.coverage
.coverage.*
/coverage-python/

# Node / Nx
node_modules/
.nx/
dist/

# Scratch space the recipes use
/.just-tmp/

# The scripted OctoPrint environment the integration tier drives: an
# instance, its own virtual environment, its logs and its API key.
/.octoprint-env/
